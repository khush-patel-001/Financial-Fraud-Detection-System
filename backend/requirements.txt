# BACKEND DEPENDENCIES
# ====================
# These are all the Python libraries our backend needs to run

# Web Framework
flask==3.0.0              # Main web framework for building our API
flask-cors==4.0.0         # Allows frontend to communicate with backend

# Machine Learning Libraries
scikit-learn==1.3.2       # Machine learning library (includes our Gradient Boosting model)
joblib==1.3.2             # For loading saved model files (.pkl)
numpy==1.24.3             # For numerical operations and arrays
pandas==2.1.4             # For handling data in DataFrame format
imbalanced-learn==0.11.0  # For handling class imbalance (used in training)

# Additional utilities
python-dotenv==1.0.0      # For environment variables (optional)
